{
  "name": "zk-report-puller",
  "version": "1.0.0",
  "private": true,
  "description": "Pull attendance from ZKTeco devices (ADMS push + TCP pull) and build attendance reports. Read-only toward devices.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "express": "^4.21.2",
    "node-zklib": "^1.3.0",
    "xlsx": "^0.18.5"
  }
}
