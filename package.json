{
  "name": "zk-report-puller",
  "version": "1.0.0",
  "private": true,
  "description": "Attendance reports web app, backed by Supabase. Deployable on Vercel or run locally; punches are collected separately by cloud/agent (runs on-site, on the same network as the ZKTeco devices).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.2",
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.4.2",
    "express": "^4.21.2",
    "xlsx": "^0.18.5"
  }
}
